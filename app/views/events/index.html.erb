<div id="announcement_root">
<h1><%= @event.name %></h1>
<div id="mainphoto">
  <%= link_to image_tag(@event.photo.url(:original)), event_path(@event) %>
</div>
<div class="desc"><%= (@event.description).split("\n")[0..2] %><br/></div>
</div>